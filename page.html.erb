<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Robespierre &amp; the Reign of Cher Showcase Tracker</title>
    <link rel="stylesheet" type="text/css" href="style.css" />
  </head>
  <body>
    <div class="main">
      <div id="showcase-progress">
        <div class="title">Showcase Progress!</div>
        <div class="subtitle">Go Kart</div>
        <p>Currently: <%= progress['name'] %></p>
        <progress value="<%= ENV['PROGRESS_STAGE'].to_i + 1 %>" max="5"></progress>
      </div>
      <h1>Send Encouragement!</h1>
      <form class="" action="/" method="post" />
        <input type="textarea" name="message" value="Go BJ Scots!" />
        <input type="submit" value="submit" />
      </form>
      <div id="messages">
        <div class="title">Messages</div>
        <% messages.each do |message| %>
          <ul>
            <%= message[:content] %> <br /> -- <%= message[:created_at] %>
          </ul>
        <% end %>
      </div>
    </div>
  </body>
</html>